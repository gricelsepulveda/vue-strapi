{
    "compilerOptions": {
        "target": "es5",  //browser compatibility
        "strict": true, //this context inference
        // dead code optimization (tree shaking)
        "module": "es2015", 
        "moduleResolution": "node"
    }
}